apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: modelmanager-backend
        # CHANGED: Replaced ':latest' with a stable placeholder for the CI/CD pipeline.
        image: richardjoseph1/modelmanager-backend-kube-openwebui:f3a6d2c9ee6140c28935d1fde99c1831e990b964
        
        ports:
        - containerPort: 8080
        env:
        - name: OLLAMA_HOST
          value: "http://ollama:11434"
        resources:
          requests:
            cpu: "10m"
            memory: "256Mi"
          limits:
            cpu: "25m"
            memory: "512Mi"
        livenessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /api/models
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10