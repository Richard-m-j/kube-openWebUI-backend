services:
  # The Ollama AI Engine
  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    networks:
      - app-network

  # The Go Backend Service
  backend:
    container_name: backend
    build:
      context: ./kube-openWebUI-backend
    environment:
      - OLLAMA_HOST=http://ollama:11434
    depends_on:
      - ollama
    networks:
      - app-network

  # Your custom Vue.js Frontend Service
  frontend:
    container_name: kube-openwebui-frontend
    build:
      context: ./kube-OpenWebUI-frontend
    ports:
      # Exposes your custom app on the standard HTTP port 80
      - "80:80"
    depends_on:
      - backend
    networks:
      - app-network

  # The OpenWebUI Service (NEW!)
  open-webui:
    image: ghcr.io/open-webui/open-webui:main
    container_name: open-webui
    ports:
      # Runs on port 8080 to avoid conflicting with your frontend
      - "8080:8080"
    environment:
      - 'OLLAMA_BASE_URL=http://ollama:11434'
    volumes:
      - open_webui_data:/app/backend/data
    depends_on:
      - ollama
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  ollama_data:
  open_webui_data: # <-- Added volume for OpenWebUI
